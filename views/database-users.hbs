<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{pageTitle}} - Database Admin</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <style>
        .sidebar {
            min-height: 100vh;
            background: #343a40;
        }
        .sidebar .nav-link {
            color: #adb5bd;
        }
        .sidebar .nav-link:hover,
        .sidebar .nav-link.active {
            color: #fff;
            background: #495057;
        }
        .table-actions .btn {
            padding: 0.25rem 0.5rem;
            font-size: 0.875rem;
        }
        .badge-admin {
            background-color: #dc3545;
        }
        .badge-user {
            background-color: #28a745;
        }
    </style>
</head>
<body>
    <div class="container-fluid">
        <div class="row">
            <!-- Sidebar -->
            <div class="col-md-2 px-0">
                <div class="sidebar">
                    <div class="p-3">
                        <h4 class="text-white">
                            <i class="fas fa-database"></i> DB Admin
                        </h4>
                    </div>
                    <nav class="nav flex-column">
                        <a class="nav-link" href="/admin/database">
                            <i class="fas fa-tachometer-alt"></i> Dashboard
                        </a>
                        <a class="nav-link active" href="/admin/database/users">
                            <i class="fas fa-users"></i> Users
                        </a>
                        <a class="nav-link" href="/admin/database/courses">
                            <i class="fas fa-book"></i> Courses
                        </a>
                        <a class="nav-link" href="/admin/database/modules">
                            <i class="fas fa-list"></i> Modules
                        </a>
                        <a class="nav-link" href="/admin/database/purchases">
                            <i class="fas fa-shopping-cart"></i> Purchases
                        </a>
                        <a class="nav-link" href="/admin/database/progress">
                            <i class="fas fa-chart-line"></i> Progress
                        </a>
                        <hr class="my-3">
                        <a class="nav-link" href="/login">
                            <i class="fas fa-home"></i> Back to Grocademy
                        </a>
                        <a class="nav-link" href="/docs">
                            <i class="fas fa-book"></i> API Documentation
                        </a>
                    </nav>
                </div>
            </div>

            <!-- Main Content -->
            <div class="col-md-10">
                <div class="p-4">
                    {{#if error}}
                    <div class="alert alert-danger">
                        <i class="fas fa-exclamation-triangle"></i> {{error}}
                    </div>
                    {{else}}
                    
                    <!-- Page Header -->
                    <div class="d-flex justify-content-between align-items-center mb-4">
                        <h1><i class="fas fa-users"></i> Database Users</h1>
                        <div>
                            <span class="badge bg-primary">Total: {{pagination.total}} users</span>
                        </div>
                    </div>

                    <!-- Search Form -->
                    <div class="card mb-4">
                        <div class="card-body">
                            <form method="GET" class="row g-3">
                                <div class="col-md-6">
                                    <label for="search" class="form-label">Search Users</label>
                                    <input type="text" class="form-control" id="search" name="search" 
                                           value="{{search}}" placeholder="Search by username, email, or name...">
                                </div>
                                <div class="col-md-3">
                                    <label for="limit" class="form-label">Per Page</label>
                                    <select class="form-select" name="limit">
                                        <option value="10" {{#if (eq pagination.limit 10)}}selected{{/if}}>10</option>
                                        <option value="20" {{#if (eq pagination.limit 20)}}selected{{/if}}>20</option>
                                        <option value="50" {{#if (eq pagination.limit 50)}}selected{{/if}}>50</option>
                                        <option value="100" {{#if (eq pagination.limit 100)}}selected{{/if}}>100</option>
                                    </select>
                                </div>
                                <div class="col-md-3 d-flex align-items-end">
                                    <button type="submit" class="btn btn-primary me-2">
                                        <i class="fas fa-search"></i> Search
                                    </button>
                                    <a href="/admin/database/users" class="btn btn-outline-secondary">
                                        <i class="fas fa-times"></i> Clear
                                    </a>
                                </div>
                            </form>
                        </div>
                    </div>

                    <!-- Users Table -->
                    {{#if users}}
                    <div class="card">
                        <div class="card-body">
                            <div class="table-responsive">
                                <table class="table table-hover">
                                    <thead class="table-dark">
                                        <tr>
                                            <th>Username</th>
                                            <th>Email</th>
                                            <th>Name</th>
                                            <th>Role</th>
                                            <th>Courses</th>
                                            <th>Progress</th>
                                            <th>Joined</th>
                                            <th>Actions</th>
                                        </tr>
                                    </thead>
                                    <tbody>
                                        {{#each users}}
                                        <tr>
                                            <td>
                                                <strong>{{this.username}}</strong>
                                                {{#if this.is_admin}}
                                                <i class="fas fa-crown text-warning ms-1" title="Admin"></i>
                                                {{/if}}
                                            </td>
                                            <td>{{this.email}}</td>
                                            <td>{{this.first_name}} {{this.last_name}}</td>
                                            <td>
                                                {{#if this.is_admin}}
                                                <span class="badge badge-admin">Admin</span>
                                                {{else}}
                                                <span class="badge badge-user">User</span>
                                                {{/if}}
                                            </td>
                                            <td>
                                                <span class="badge bg-info">{{this.courseCount}}</span>
                                            </td>
                                            <td>
                                                <span class="badge bg-success">{{this.progressCount}}</span>
                                            </td>
                                            <td>
                                                <small>{{formatDate this.created_at 'MMM DD, YYYY'}}</small>
                                            </td>
                                            <td class="table-actions">
                                                <a href="/admin/database/user/{{this.id}}" 
                                                   class="btn btn-sm btn-outline-primary" title="View Details">
                                                    <i class="fas fa-eye"></i>
                                                </a>
                                                <a href="/admin/database/purchases?userId={{this.id}}" 
                                                   class="btn btn-sm btn-outline-info" title="View Purchases">
                                                    <i class="fas fa-shopping-cart"></i>
                                                </a>
                                                <a href="/admin/database/progress?userId={{this.id}}" 
                                                   class="btn btn-sm btn-outline-success" title="View Progress">
                                                    <i class="fas fa-chart-line"></i>
                                                </a>
                                            </td>
                                        </tr>
                                        {{/each}}
                                    </tbody>
                                </table>
                            </div>
                        </div>
                    </div>

                    <!-- Pagination -->
                    {{#if (gt pagination.totalPages 1)}}
                    <nav class="mt-4">
                        <ul class="pagination justify-content-center">
                            <li class="page-item {{#if (eq pagination.page 1)}}disabled{{/if}}">
                                <a class="page-link" href="?page={{subtract pagination.page 1}}{{#if search}}&search={{search}}{{/if}}{{#if pagination.limit}}&limit={{pagination.limit}}{{/if}}">
                                    <i class="fas fa-chevron-left"></i>
                                </a>
                            </li>
                            
                            {{#each (range 1 (add pagination.totalPages 1))}}
                            <li class="page-item {{#if (eq this ../pagination.page)}}active{{/if}}">
                                <a class="page-link" href="?page={{this}}{{#if ../search}}&search={{../search}}{{/if}}{{#if ../pagination.limit}}&limit={{../pagination.limit}}{{/if}}">
                                    {{this}}
                                </a>
                            </li>
                            {{/each}}
                            
                            <li class="page-item {{#if (eq pagination.page pagination.totalPages)}}disabled{{/if}}">
                                <a class="page-link" href="?page={{add pagination.page 1}}{{#if search}}&search={{search}}{{/if}}{{#if pagination.limit}}&limit={{pagination.limit}}{{/if}}">
                                    <i class="fas fa-chevron-right"></i>
                                </a>
                            </li>
                        </ul>
                        <div class="text-center text-muted">
                            Showing {{add (multiply (subtract pagination.page 1) pagination.limit) 1}} to 
                            {{#if (lt (multiply pagination.page pagination.limit) pagination.total)}}
                                {{multiply pagination.page pagination.limit}}
                            {{else}}
                                {{pagination.total}}
                            {{/if}} 
                            of {{pagination.total}} users
                        </div>
                    </nav>
                    {{/if}}
                    
                    {{else}}
                    <div class="text-center py-5">
                        <i class="fas fa-users fa-3x text-muted mb-3"></i>
                        <h4 class="text-muted">No users found</h4>
                        <p class="text-muted">Try adjusting your search criteria.</p>
                    </div>
                    {{/if}}

                    {{/if}}
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
